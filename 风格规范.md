# Rule of MotorCore

本文档描述该项目的开发宗旨与编写准则

---

## 总则

### 元法则与三原则

**元法则：以用户为中心。**
即**任何新增/修改的内容都能被用户以极低的学习成本直接用于二开/调试/修改**。

具体体现在：**可读性**，**灵活性**，**轻量性**。

### 工程落地约定

- **可读性优先**：他人（和半年后的自己）能较快理解意图与物理含义。
- **标识符用英文，注释可用中文**：电机控制里中文注释便于对齐现场说法。
- **同一模块内风格一致**：命名、缩进、`switch` 写法等不要同文件混用多种习惯。
- **改动聚焦**：不借风格之名做大范围无关重构；能改一行解决的不要改一百行。

---

## 可读性

### 结构体嵌套量

定义的结构体嵌套控制在 **2 层以内**，最多不超过 **3 层**，因为人类能自然感知到的最大数字为 **4±1**。

### 命名总则（函数与变量）

- **函数命名**：前缀 `_类1_类2_...`，小写 + 下划线，整体为**动词开头**的英文短语。例如 `ctrl_motor_openloop_velocity_el_nonblock`：前缀 `ctrl`（控制）+ 最大类 `motor`（电机）+ `openloop`（开环）等。
- **中文正序 → 英文倒序**：把函数功能用**中文正序**写出来，其**英文单词倒序**即函数名。例如 `ctrl_motor_openloop_velocity_el_nonblock` 的中文正序含义为：**非阻塞式电速度开环电机控制**。
- **前缀语义**（与对外 API 一致时优先采用；与同文件既有风格冲突时以既有为准）：

| 前缀 | 含义 | 输入/过程/输出特征 | 示例 |
| --- | --- | --- | --- |
| `update` | 更新数据 | 可有/可无输入 + 有实质过程 + 输出为数值或数值与副作用 | `update_angle_el`：计算并写入 `motor->MotorAlg.angle_el` |
| `get` | 获取数据 | 无输入 + 无实质过程 + 输出为数值 | `get_angle_el`：仅从句柄读出并返回 |
| `calculate` | 对输入做处理并返回 | 有数值输入 + 有实质过程（常为运算）+ 输出数值 | `calculate_angle_el`：由采样等算电角 |
| `set` | 输出动作 | 有输入 + 有实质过程 + 输出为**动作**（如对桥臂的控制） | `set_svpwm` |
| `ctrl` | 一系列动作 | 常为多个 `set` 的组合 | `ctrl_motor_openloop_velocity_el_nonblock` |

- **变量命名**：与函数命名思路一致，但**无上述功能前缀**；同样可用「中文正序 → 英文倒序」辅助记忆，例如 `velocity_lpf` 表示低通滤波后的速度。项目中仍有部分历史变量未完全统一，后续迭代逐步修改。

### 文件与目录

| 类型              | 规则          | 示例                         |
| ----------------- | ------------- | ---------------------------- |
| 自研源/头文件     | 小写 + 下划线 | `foc_alg.c`, `foc_drv.h` |
| Cube/HAL 生成文件 | 不强行改名    | `stm32g4xx_it.c`           |

### 类型

| 规则                       | 示例                                             |
| -------------------------- | ------------------------------------------------ |
| 小型 typedef               | `Time_t`, `PID_t`, `SMA_t`                 |
| 与 HAL 对齐的大型句柄/配置 | `Motor_HandleTypeDef`, `Motor_ConfigTypeDef` |

新建类型：可优先 `模块_语义_t`；若明确对齐 HAL 风格，可用 `*TypeDef`。

### 函数（补充约定）

- 对外 API 可加模块前缀（如 `foc_init`、`stm32_update_dt`）避免符号冲突。
- **与当前 FOC 对齐**：硬件采样并写回句柄用 `update_*`；纯数学/坐标变换可用 `calculate_*` 或 `Calculate_*`（**与同文件内既有风格一致即可**）。
- **成对**：`*_block` / `*_nonblock`（校准、辨识等流程）。

### 变量与成员

- 局部/静态变量：小写 + 下划线，如 `velocity_raw`、`angle_now`。
- 物理量易混时（机械角/电角、法兰侧等）在命名或注释中标明含义与单位习惯。
- 结构体新字段建议逐步统一为小写 + 下划线；与遗留大写字段并存时，新代码不继续引入第三种风格。

### 宏与常量

- 全大写 + 下划线：`PI`、`I_ADC_CONV`、`A_PWM_Period`。
- 头文件保护避免 `__` 前缀，例如：`FOC_ALG_H`。

### 拼写与用词

- 参数名避免笔误（如 `from_low` / `from_high` 优于 `formLOW`）。
- 同一概念在全仓库保持一种拼写（如 `pole_pairs` 相关字段）。

### 注释与文档

- **头文件**：对对外结构体、重要 API 使用简短说明（可采用 `@brief` / `@note`）；复杂字段用行注释说明物理意义、单位、使用前提。
- **源文件**：关键算法步骤（Clark/Park、扇区、速度 unwrap、校准状态机）保留说明；**设计约束**（例如「同一控制周期内角度只应更新一次」）可写在注释里，长期建议用代码结构保证。
- **注释掉的旧实现**：若保留，建议简短标注原因（对比实验 / 待删 / 参考），避免大块无声注释淹没主逻辑。
- **调试输出**：`printf` / RTT 等调试语句在合入主分支前尽量移除或加编译开关，避免风格上依赖控制台。

### 格式与排版

- **缩进**：同一文件内统一使用空格或 Tab，避免混排。
- **行宽**：无硬性要求；过长表达式可换行对齐，便于 diff 阅读。
- **`switch`**：`case` 与 `break` 风格在同文件内统一（例如是否使用 `} break;` 一体写法）。
- **大文件**：单文件职责过多时（算法 + 多种校准 + 开环），新功能优先考虑拆到独立 `.c` 或分区，属于风格与可维护性的折中。

---

## 灵活性

### 元函数

将每个动作/内容/功能都封装成一个函数：**任何函数只负责一个动作/内容/功能**，不做多余的事情。包含大量具体动作的函数只负责**调用与组合**下级函数；下级再同样分解。这样用户有二开/调试/修改需要时，可直接调用对应**元函数**，避免牵一发而动全身的耦合。

例如 `update_velocity_LPF`：只负责调用 `Calculate_velocity_raw` 与 `Calculate_LPF` 等，把「更新滤波后速度」分解为原始速度计算 + 低通，自身只做编排与必要衔接。

截至 2026-05-02，项目中仍有部分函数（如 `update_angle`）未完成元函数化拆分；**本文档规范未来方向**，改动相关模块时逐步靠拢。

### 架构与模块习惯

- **分层**：算法与硬件解耦——算法层通过句柄 + 函数指针（如 `Motor_DrvTypeDef`）访问 PWM、ADC、编码器、延时、`dt` 等；板级实现放在 `foc_drv.c` 或同类文件中。
- **数据聚合**：与电机相关的配置、算法状态、采样与校准数据集中在句柄结构体（如 `Motor_HandleTypeDef`）中，避免散落全局变量。
- **阻塞与非阻塞成对**：校准、辨识等流程提供 `*_block`（便于调试/产线）与 `*_nonblock`（便于主循环或状态机），命名上成对出现。
- **依赖方向**：尽量避免头文件互相包含；公共类型放在基础头文件，驱动头文件按需包含算法头文件时保持单向依赖。

---

## 轻量性

**能一句话写完绝不多定义几个变量。**
实现上注意：控制周期内阈值等**魔数**建议逐步收拢为具名常量或配置字段，并在注释中说明来源（实验/手册），与「少变量」不矛盾——避免的是无意义的中间堆砌，而非拒绝有意义的命名常量。

---

## C 语言与嵌入式习惯

- **条件判断**：布尔条件使用 `&&` / `||`；不要对指针判空等使用按位 `&` / `|`。
- **返回静态缓冲区的函数**（如返回 `float*` 指向 `static` 数组）：调用方避免在同一表达式内链式多次调用；注意不可重入，ISR 与主循环共用时要格外谨慎。
- **多实例**：PID、校准状态机等避免使用跨调用 `static` 保存「本应属于某个电机」的状态；状态进句柄或显式上下文结构体。
- **动态内存**：在 MCU 上 `calloc`/`free` 仅用于低频路径（如校准）；注意失败分支与 `free` 配对，避免泄漏。

---

## FOC / 电机控制相关风格

- **角度**：机械角、电角、多圈累计（如 `angle_all`）分工明确；unwrap 与速度估计逻辑与 `update_angle` 系列保持一致，避免多处重复且不一致的阈值。
- **坐标变换**：三角函数在性能敏感路径优先与现有代码一致（如 CMSIS-DSP `arm_sin_f32` / `arm_cos_f32`）。
- **PWM 与相序**：相线映射（`DIR`）与电流采样映射在同一风格下维护，修改一处要想到另一处。

---

## 修订记录

| 日期       | 说明                                                                                 |
| ---------- | ------------------------------------------------------------------------------------ |
| 2026-05-02 | 与《MotorCore编写准则》对齐结构并合并《风格规范》细则；《MotorCore编写准则》原文保留 |
| 2026-04-02 | 由《命名规范》扩展为《风格规范》，补充架构、注释、格式与嵌入式约定                   |
| 2026-04-02 | 初版命名相关条目，基于 FOC 模块现状                                                  |
